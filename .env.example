# Copy this file to .env and adjust as needed:
#   cp .env.example .env
#
# Base URL of the deployed SentinelOps ASP.NET Core API + SignalR hub.
# If unset, the app falls back to http://localhost:5283 for local development.
VITE_API_BASE_URL=http://localhost:5283
